{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "gdb",
      "request": "launch",
      "name": "Launch Program",
      "target": "${workspaceRoot}/target/debug/discord-markov-bot",
      "cwd": "${workspaceRoot}"
    },
    {
      "name": "Debug",
      "type": "gdb",
      "request": "launch",
      "target": "${workspaceRoot}/target/debug/discord-markov-bot",
      "cwd": "${workspaceRoot}"
    }
  ]
}