{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "gdb",
      "request": "launch",
      "name": "GDB",
      "target": "${workspaceRoot}/target/debug/discord-markov-bot",
      "cwd": "${workspaceRoot}"
    },
    {
      "name": "LLDB",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/discord-markov-bot",
      "cwd": "${workspaceRoot}"
    }
  ]
}